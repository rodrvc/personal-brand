# Brand spec: `<brand name>`

- **Captured on:** `<YYYY-MM-DD>`
- **Source(s):** `<e.g. production frontend CSS file, official website, brand guide PDF/URL, user-provided files>`
- **Completeness:** `<full | partial | inferred>`

---

## Primary assets

### Logo
- **File(s):** `<path or link to logo asset(s)>`
- **Light-background variant:** `<describe or link>`
- **Dark-background variant:** `<describe or link, or "none found — flag as gap">`
- **Permitted uses:** `<e.g. "wordmark text only, no image asset exists">`
- **Forbidden uses:** `<e.g. "never recolor", "never use logo font for body copy">`

### Photography / product renders
- **Available:** `<yes/no>`
- **Source(s):** `<where these came from>`
- **Notes:** `<licensing, resolution, anything that limits how these can be used>`

### UI screenshots
- **Available:** `<yes/no — n/a if not a product/app>`
- **Source(s):** `<where these came from>`
- **Verified clean of third-party/demo contamination:** `<yes/no>`

---

## Auxiliary assets

### Color palette

| Color | Hex | Usage | Source |
|---|---|---|---|
| `<name>` | `<#hex>` | `<e.g. primary/brand, accent/CTA, background>` | `<exact file/page this was verified against>` |

### Typography

| Role | Typeface | Notes |
|---|---|---|
| Display/logo | `<font name>` | `<e.g. reserved for wordmark only, never for body copy>` |
| Body | `<font name>` | |
| Mono (if any) | `<font name>` | `<or "none">` |

### Vibe keywords

`<3-5 words that capture the intended feeling — used later to pick a direction from references/style-library.md>`

---

## Completeness notes

`<What's verified vs. inferred vs. missing. If something was assumed provisionally, say what was assumed and what would resolve it (e.g. "no dark-mode logo variant found — assumed white wordmark on brand-color background is acceptable until confirmed").>`
